char *a="0x41";
	int b;
	sscanf(a,"%x",&b);

	AnsiString Datta;
	char *Command;
	//int a = 0x31;
	Datta =  IntToHex(1, 0).c_str();
	Command = Datta.c_str();

	//sprintf(Command, "%02X",&a);
	Memo1->Lines->Add(" омманда " + String(b) + " успешно передана в порт ");
	Memo1->Lines->Add(" омманда" + String(Command) + "успешно передана в порт ");


	AnsiString Datta;
	char *Command;
	Datta =  IntToHex(1, 0).c_str();
	Command = Datta.c_str();


AnsiString Datta = ;
	char *Command;
	char *a="0x02";



	char *a = "0x01";
	Write_mode(a);
	Read_mode();

FG - защитное заземление (экран).
TxD - данные, передаваемые компьютером в последовательном коде (логика отрицательна€).
RxD - данные, принимаемые компьютером в последовательном коде (логика отрицательна€).
RTS - сигнал запроса передачи. јктивен во все врем€ передачи.
CTS - сигнал сброса (очистки) дл€ передачи. јктивен во все врем€ передачи. √оворит о готовности приемника.
DSR - готовность данных. »спользуетс€ дл€ задани€ режима модема.
SG - сигнальное заземление, нулевой провод.
DCD - обнаружение несущей данных (детектирование принимаемого сигнала).
DTR - готовность выходных данных.
RI - индикатор вызова. √оворит о приеме модемом сигнала вызова по телефонной сети.
Alena-Popova
src/main\java\ru\itmo